# Assignment 2 — handout starter (empty skeleton)

This is the **unmodified Assignment-2 handout template**, not my solution. In
`execute_crypto.py` every method body is still the placeholder
`# Write your script here` — no algorithms are implemented here. I kept it for
reference so the starting point is visible.

My **completed** A2 implementation and report are in
[`../SIL_ASS2/script/`](../SIL_ASS2/script/). See the assignment
[`README.md`](../README.md) for the full write-up.

## Contents

- `execute_crypto.py` — the empty `ExecuteCrypto` skeleton (handout framework).
- `setup_env.sh` — a package-install stub.
- `original_plaintext.txt` — the A2 input plaintext.
- `examples/` — handout-provided sample keys, ciphertexts, and auth tags.
